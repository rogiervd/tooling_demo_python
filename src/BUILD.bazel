# The main library that this repo pretends to provide.
py_library(
    name = "compute",
    srcs = [
        "compute/__init__.py",
        "compute/add_two.py",
    ],
    imports = ["."],
    visibility = ["//visibility:public"],
)
